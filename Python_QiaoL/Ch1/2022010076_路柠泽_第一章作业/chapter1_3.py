import math
#in order to use the factorial function
a=int(input("Enter the first integer: "))
b=int(input("The second: "))
#provide the input prompts for the two numbers
print(math.factorial(a)+math.factorial(b))
#calculate and output